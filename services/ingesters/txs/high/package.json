{
  "name": "pioneer-ingester-txs-low",
  "version": "1.0.0",
  "scripts": {
    "npm": "npm i",
    "lint": "prettier --write '**/**/*.ts'",
    "start": "node lib/index",
    "build": "tsc -p .",
    "dev": "npm run build && node lib/index.js",
    "test": "npm run build && node __tests__/test-module.js",
    "prepublish": "rm -R lib && npm run build",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install",
    "docker-build": "docker build -t pioneer/pioneer-eth-block-ingester-high:latest ."
  },
  "dependencies": {
    "@pioneer-platform/blockbook": "^8.1.20",
    "@pioneer-platform/default-mongo": "^8.1.5",
    "@pioneer-platform/eth-audit": "^8.1.17",
    "@pioneer-platform/eth-network": "^8.1.20",
    "@pioneer-platform/loggerdog": "^8.1.5",
    "@pioneer-platform/redis-queue": "^8.1.5",
    "chai": "^4.2.0",
    "datadog-metrics": "^0.8.1",
    "dotenv": "^8.1.0",
    "wait-promise": "^0.4.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^13.13.12",
    "nodemon": "^2.0.3",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
